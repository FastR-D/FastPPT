# Web Application

The shared browser UI for local and server modes. It will contain project
selection, page preview, page-scoped chat, multi-page/global edit confirmation,
version history, export status, and PPTX/SVG preview labels.
