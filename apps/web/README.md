# Web Application

The same dependency-free workbench is served in local and server modes. Its
first screen exposes projects, pages, source ingestion, page-entry planning,
visual/quick/authoritative preview states, version and QA inspection, scoped
natural-language edits, confirmation, and PPTX export. The browser only sees
opaque artifact IDs and authenticated download routes.
