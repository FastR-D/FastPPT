# Agent Harness

The FastPPT-owned harness presents one structured planning contract over the
Claude Code and Codex SDKs. Agent and model configuration is injected by the
runtime; official endpoints and compatible relays use the same server-only
settings. SDK output is untrusted until FastPPT Core validates the plan.
