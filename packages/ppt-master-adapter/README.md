# ppt-master Adapter

The only application-facing boundary for the upstream `ppt-master` kernel.
Define stable contracts for SVG input, editable PPTX conversion, static QA,
and error reporting. Keep upstream implementation files in their original
locations and avoid duplicated wrappers.
