# macOS local deployment (experimental)

macOS is an experimental v1.2.0 target. These scripts manage the dependency-
free local runtime and ordinary worker. They do not claim PowerPoint authority;
without a real PowerPoint adapter the UI reports a degraded render status.

Run `./install.sh`, then `./start.sh`. A real Mac and Office installation have
not been part of the release validation environment.
