# Local Deployment

Local profile for development and offline-friendly single-user workspaces.
It may use a local Gateway and filesystem, but production code must not depend
on Codex App, Skill, Harness, MCP, or a hard-coded localhost service.
