# FastPPT 项目工作流等探讨

## 项目工作流

FastPPT 结合 `ppt-master` 与实际项目经验，采用以下工作流：

```text
输入
  -> 内容与视觉规划，或直接复用已有工作产物
  -> 生成可编辑 SVG 页面
  -> SVG 检查
  -> SVG 转换为 PPTX
  -> PPTX 后置检查
  -> 本地 Web 预览、单页修改与导出
```


## Skill 路由

Skill 必须先检查用户已经提供的工件，只运行缺失的阶段：

| 用户输入或意图 | 从此阶段开始 |
| --- | --- |
| 主题、题目或原始资料 | 内容规划与主题选择。 |
| 已有大纲或页面编排 | 视觉方案确认，跳过内容规划。 |
| 已有页面视觉图，要求“转可编辑 PPT” | 图片重建为 SVG，跳过规划和图像生成。 |
| 已有 FastPPT 项目 | 读取现有 SVG，按用户要求编辑目标页。 |
| 已有 FastPPT 导出 PPTX，且项目工件仍在 | 从项目 SVG 继续编辑，不反向解析 PPTX。 |
| 上传非 FastPPT PPTX，要求“重构”“换风格”或“按此内容重做” | 将其作为内容和视觉参考，重新生成 FastPPT 页面。 |
| 上传已有 PPTX，要求保留原生设计并编辑 | 进入 `workflows/routing.md` 选择的 Edit Native PPTX 路线，在 round-trip 工作区只重建计划修改的对象或页面。 |

这套路由确保 Skill 不会强迫用户重复已经完成的内容规划、图像生成或格式转换步骤。

## 本地 Web

### 技术栈与职责

- Web 使用 Vite、React技术栈
- 服务只负责读取和写入项目文件，不保存 API Key

### 界面功能

界面包含以下功能：

- 页面缩略图；
- SVG 主画布；
- 对象或区域选框；
- 对话输入与审批；
- 撤销与版本管理；
- 导出状态与下载入口。

用户点击对象时，界面直接获取 SVG 对象 ID；用户框选区域时，界面记录该区域的页面坐标。

### 修改请求约束

每个修改请求必须包含以下信息：

- 页号；
- 修改目标；
- `baseRevision`；
- 修改意图。

出现以下任一情况时，必须先要求用户确认：

- 页面版本已过期；
- 修改目标不明确；
- 修改可能影响其他页面。

### 单页修改流程

```text
备份当前页面
  -> 修改目标 SVG 或图片
  -> SVG 检查
  -> 导出并检查
  -> 更新版本
```

撤销操作应恢复上一个通过检查的页面版本。任一页面发生改变后，整套 PPTX 的导出状态都必须标记为“已过期”。
